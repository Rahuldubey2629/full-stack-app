
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devpulse-pvc
  namespace: devpulse
  labels:
    app: devpulse-pvc
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:  
    requests:
      storage: 1Gi
