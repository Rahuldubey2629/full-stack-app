apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: devpulse
  labels:
    app: devpulse-backend
spec:
  selector:
    app: devpulse-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP